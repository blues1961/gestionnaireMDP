# ⚠️ EXEMPLE — NE PAS METTRE DE VRAIS SECRETS ICI

# ============================
# Secrets communs DEV/PROD
# ============================
POSTGRES_PASSWORD=changeme
DJANGO_SECRET_KEY=changeme
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeMe!
ADMIN_EMAIL=admin@example.com
API_AUTH_USERNAME=sync-bot
API_AUTH_PASSWORD=changeMeApi!

# ============================
# Accès SSH + chemins PROD (scripts push/pull/pull-backup)
# ============================
PROD_SSH_HOST=linode
PROD_DIR=/opt/apps/${APP_SLUG}
PROD_ENV_LOCAL_PATH=${PROD_DIR}/.env.local
PROD_BACKUPS_DIR=${PROD_DIR}/backups

# Répertoires locaux (DEV)
DEV_DIR=${HOME}/projets/${APP_DEPOT}
BACKUP_DIR=backups
DEV_BACKUPS_DIR=${BACKUP_DIR}

# ============================
# Connexion DB PROD (dump -> DEV)
# ============================
PROD_DB_HOST=${POSTGRES_HOST}
PROD_DB_PORT=${POSTGRES_PORT}
PROD_DB_NAME=${POSTGRES_DB}
PROD_DB_USER=${POSTGRES_USER}
PROD_DB_PASSWORD=${POSTGRES_PASSWORD}
