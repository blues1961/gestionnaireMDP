# Node / Vite
frontend/node_modules/
frontend/dist/

# Python cache
**/__pycache__/
*.pyc

# Django/SQLite (dev)
backend/db.sqlite3

# Backups & dumps (garder le dossier via .gitkeep)
backups/**
!backups/.gitkeep
*.sql
*.sql.gz
*.tar.gz
*.dump

# Secrets & env (ne jamais versionner, sauf les templates)
.env
.env.*
!.env.dev
!.env.prod
!.env.example
!.env.dev.example
!.env.prod.example
.env.*.local
.env.local
.envrc
!.env.local.example

# Cookies / tokens
*.cookies
cookies.txt
.cookies.dev

# Fichiers temporaires & .bak
*.bak
*.bak*
*.BAK.*
*.BROKEN.*
*.swp
*.swo
project-tree-*.txt

# Docker (dev-only helpers)
docker-compose.yml.bak*
docker-compose.dev.override.yml

# Dev-only (nginx)
frontend/nginx.dev.conf
frontend/nginx.resolver.conf
frontend/.env.development
frontend/.env.development.local
dist/
